export const base_url = "https://rickandmortyapi.com/api";

export const CHARACTER = `${base_url}/character`

export const CHARACTER_ALIVE = `${base_url}/character/?status=alive`

export const CHARACTER_DEAD = `${base_url}/character/?status=dead`

export const CHARACTER_MALE = `${base_url}/character/?gender=male`

export const CHARACTER_FEMALE = `${base_url}/character/?gender=female`